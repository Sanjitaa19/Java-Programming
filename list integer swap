class Solution {
    static List<Integer> get(int a, int b) {
      int temp = a;
        a = b;
        b = temp;

        // Create list and add swapped values
        List<Integer> result = new ArrayList<>();
        result.add(a);
        result.add(b);

        return result;
    }
}

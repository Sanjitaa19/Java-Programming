import java.util.Scanner;

public class OddIntegersInRange {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int X = scanner.nextInt();
        int Y = scanner.nextInt();
        
        for (int i = X + 1; i < Y; i++) {
            if (i % 2 != 0) {
                System.out.print(i + " ");
            }
        }
        
        scanner.close();
    }
}
